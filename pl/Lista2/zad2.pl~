
jednokrotnie(X, L) :-
    select(X, L, Rest),
    \+ member(X, Rest).


%po prawej stronie X , znak X wystepuje dokladnie raz

dwukrotnie(X,L):-
    append(_, [X|Right], L),
    jednokrotnie(X, Right).



